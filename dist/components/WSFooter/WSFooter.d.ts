import { FC, ReactNode } from 'react';
interface WSFooterProps {
    children?: ReactNode;
    className?: string;
    regardingWeskill?: ReactNode;
    courses?: ReactNode;
    contacts?: ReactNode;
}
declare const WSFooter: FC<WSFooterProps>;
export default WSFooter;
